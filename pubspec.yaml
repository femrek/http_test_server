name: http_test_server
description: "Dart package for testing HTTP requests and responses. Creates a simple HTTP server that responds with a specified response."
version: 0.0.1
homepage:

environment:
  sdk: ^3.6.0

dev_dependencies:
  test: ^1.25.14
  very_good_analysis: ^7.0.0
